[project]
name = "twincat-traffic-light"
version = "0.1.0"
description = "TwinCAT trafik lambası PLC eğitim projesi ve Python simülasyonu"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py311"
